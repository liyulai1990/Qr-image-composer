# 开发者手册 · 图片二维码合成工具

纯前端批量图片合成工具（单文件）

> 版本：2.0（2026-08）
> 交付文件：`index.html`

---

## 1. 运行方式

- 双击 `index.html` 直接在浏览器打开，无需服务器、无需构建。
- 兼容 Chrome 128+ / Edge 128+。
- 所有文件处理均在浏览器本地完成，不产生任何网络请求。

---

## 2. 技术构成

| 模块 | 实现 | 说明 |
|------|------|------|
| 页面结构 | 单 HTML 文件 | 无外部依赖、无 CDN、无构建工具 |
| 样式 | CSS3 + `:root` 变量 | 主题色/间距/圆角集中管理 |
| 绘图 | HTML5 Canvas 2D | 预览与导出共用 `drawQR` / `drawFixedQR` |
| 文件读取 | File API + `URL.createObjectURL` | 加载后立即 `revokeObjectURL` 防泄漏 |
| 图片解码 | `Image` + `onload` | `loadImage()` 返回 Promise |
| ZIP 打包 | 自研 store 模式编码器 | CRC32（分块）+ 本地文件头 + 中央目录 + 结尾记录 |
| 下载 | `Blob` + 临时 `<a>` | 导出后 2 秒释放 URL |
| 交互 | 原生 DOM 事件 | mousedown/mousemove/mouseup/wheel/keydown |

---

## 3. 状态模型与常量

### 3.1 常量

```js
const MAX_BASE_SIZE = 50 * 1024 * 1024;   // 底图上限 50MB
const MAX_QR_SIZE   = 30 * 1024 * 1024;   // 二维码上限 30MB
const MAX_PIXELS    = 30000000;           // 像素上限 3000 万
const MAX_QR_COUNT  = 200;                // 动态二维码数量上限
```

### 3.2 全局状态 `state`

```js
const state = {
  baseImg, baseFile, baseMime, baseObjectURL,   // 底图
  qrFiles, currentIdx,                          // 动态二维码列表与当前选中
  settings: { x, y, scale, rot, crop:{l,t,r,b} }, // 动态二维码变换参数
  fixedQRs, selectedFixedId,                    // 固定二维码（≤2）
  dragging, dragMode, dragFixedId,              // 拖拽状态
  dragOffX, dragOffY, cropEdge, cropRef,
  renderPending                                 // rAF 渲染节流标志
};
```

---

## 4. 坐标系与几何（重要）

- `settings.x / settings.y` 是二维码**轴对齐包围盒的左上角**（以底图像素为坐标）。
- 中心点：`cx = x + w/2`，`cy = y + h/2`，其中 `w = (naturalWidth - crop.l - crop.r) * scale`。
- 旋转以中心点为圆心；`cos / sin` 由 `rot * π / 180` 计算。
- `localToImg(b, lx, ly)`：把局部（未旋转）坐标变换到图像坐标。
- `pointInDynamicQR(p)`：旋转感知命中测试，与 hover 光标一致。
- **裁剪拖拽**：鼠标点先投影到以初始中心为原点的局部坐标系（`lx / ly`），再按「保持对边中点固定」反推新 crop 值与中心，四边 `cl/cr/ct/cb` 统一处理，天然兼容旋转。

```js
// 左边缘裁剪（右边缘固定）的核心公式
let newW = r.w0/2 - lx;
let nL = r.naturalWidth - r.cropR - newW/scale;
nL = clamp(nL, 0, r.naturalWidth - r.cropR - 2);
```

- `clampDynamicPos() / clampFixedPos(fq)`：位置边界收敛，保证二维码至少露出 2px，既防完全越界负坐标，也允许贴边摆放。

---

## 5. 导出与 ZIP 编码

### 5.1 导出流程

1. 每张二维码创建独立 canvas，尺寸 = 底图 `naturalWidth × naturalHeight`。
2. 依次绘制：底图 → 固定二维码 → 动态二维码。
3. `canvasToBlob(cv, baseMime, quality)` 生成 Blob。
4. 文件名后缀对齐 `baseMime`（`extForMime`），避免「.jpg 实为 PNG」错配。
5. `buildZip()` 打包并 `downloadBlob()` 下载。

```js
const quality = state.baseMime === 'image/jpeg' ? 0.99
              : state.baseMime === 'image/webp' ? 0.99
              : undefined;   // PNG 无损，忽略 quality
```

### 5.2 ZIP 结构（store 模式，无压缩）

- 文件头标志：本地 `0x04034b50`、中央 `0x02014b50`、结尾 `0x06054b50`。
- 通用标志位 `FLAGS = 0x0800`（UTF-8 文件名，支持中文）。
- 压缩方法 = 0（store）。
- `crc32()` 为异步分块实现（每 1MB `await sleep(0)` 让出事件循环），CRC 表 `CRC_TABLE` 为模块级常量。
- `buildZip()` 逐个读 Blob 计算 CRC 后即释放，`blobParts` 直接引用原 Blob，内存峰值仅约为单文件大小。

---

## 6. 可自定义模块

### 6.1 主题

修改 `<style>` 顶部 `:root` 变量：

```css
:root {
  --primary:#2f6fed;   --primary-dark:#1a5bd7;
  --danger:#ff4d4f;    --bg:#f5f7fa;  --panel-bg:#fff;
  --panel-border:#e1e5eb; --text:#1f2937; --text-sub:#6b7280;
}
```

### 6.2 文件限制与导出质量

见 3.1 与 5.1，按需调整 `MAX_*` 常量与 `quality` 取值。

### 6.3 预览缩放范围

`zoomRange` 输入 `min="5" max="300"`，即预览缩放 5% ~ 300%。`applyZoom()` 按 `getDispScale()` 设置 canvas 的 CSS 尺寸。

---

## 7. 开发约束

- **单文件**：所有代码内联于 `index.html`，禁止外链 CDN 或分包。
- **纯前端**：禁止任何服务端接口调用、禁止上传文件。
- **格式一致**：导出格式必须与底图 `baseMime` 一致，文件名后缀随 `extForMime` 对齐。
- **内存安全**：所有 `URL.createObjectURL` 必须在 `onload`/`onerror` 后 `revoke`。
- **高分辨率底图**：预览缩放仅影响显示，导出 Canvas 始终用底图 `naturalWidth × naturalHeight`。

---

## 8. 维护要点

### 8.1 内存管理
- `loadImage()` 失败时 `revokeObjectURL`；成功返回 `url` 由调用方持有并统一释放。
- `setBaseFile()` 切换底图前释放旧 `baseObjectURL`，并用 `baseLoadToken` 令牌丢弃过期请求。
- `buildZip()` 分块读 Blob，避免一次性大 `Uint8Array`。
- 缩略图模态关闭后清空 `overviewGrid.innerHTML`。

### 8.2 性能
- `renderPreview()` 用 `requestAnimationFrame` + `renderPending` 节流。
- 批量加载/导出/打包按帧 `await sleep(0)` 让出主线程。
- `crc32()` 每 1MB 分块让出；二维码列表超过 500 行自动淘汰旧 DOM 节点。
- `MAX_QR_COUNT = 200` 限制动态二维码总量，防止下拉/列表/导出性能失控。

### 8.3 并发与交互
- `setEditLocked(locked)` 采用引用计数（`editLockCount`），导出/加载期间锁定编辑区，防并发导入。
- `cancelDrag()` 统一复位拖拽，`blur` / `document.mouseleave` / `pointercancel` 均触发，防窗口外松手「粘滞」。

### 8.4 兼容性

| API | 兼容情况 | 备注 |
|-----|---------|------|
| `canvas.toBlob` | Chrome/Edge 全支持 | — |
| `new Blob([...])` | 全支持 | — |
| `URL.createObjectURL` | 全支持 | — |
| `TextEncoder` | 全支持 | ZIP 中文文件名 |
| `requestAnimationFrame` | 全支持 | 渲染节流 |

---

## 9. 关键函数索引

| 函数 | 职责 |
|------|------|
| `setBaseFile(file)` | 底图加载、竞态令牌、像素上限校验 |
| `loadImage(file)` | 图片解码 Promise，返回 `{img, url}` |
| `addQrFiles(files)` | 批量加载二维码、数量限制、加载锁 |
| `addFixedQR(file)` | 添加固定二维码（≤2） |
| `centerQR()` | 重置动态二维码到居中、默认缩放 |
| `getDynamicBox()` | 计算动态二维码几何包围盒（含旋转角） |
| `localToImg(b, lx, ly)` | 局部坐标 → 图像坐标 |
| `pointInDynamicQR(p)` | 旋转感知命中测试 |
| `clampDynamicPos()` / `clampFixedPos(fq)` | 位置边界收敛 |
| `drawQR(ctx, img, s)` | 动态二维码绘制（裁剪/缩放/旋转） |
| `drawFixedQR(ctx, fq)` | 固定二维码绘制 |
| `_doRenderPreview()` | 实际渲染（try/catch 包裹） |
| `drawDynamicQRHandles()` | 绘制选中框 + 缩放/裁剪手柄 |
| `cancelDrag()` | 统一复位拖拽状态 |
| `setEditLocked(locked)` | 引用计数编辑锁 |
| `canvasToBlob(cv, mime, q)` | `toBlob` Promise 封装 |
| `extForMime(mime)` | MIME → 文件后缀 |
| `uniqueName(name, seen)` | 文件名去重 `name (n).ext` |
| `crc32(data)` | 异步分块 CRC32 |
| `buildZip(files, onProgress)` | store 模式 ZIP 构建，返回 Blob |
| `downloadBlob(blob, name)` | 触发浏览器下载 |

---

## 10. 文件结构

```
图片二维码合成工具/
├── index.html                        # 唯一交付文件（单文件、零依赖）
├── 图片二维码合成工具-用户操作手册.md
├── 图片二维码合成工具-开发者手册.md
├── index-审计报告.md                 # 全量源码审计 + 修复清单
└── .workbuddy/memory/                # 工作记录
```

## 如有侵权或意见，可通过邮件联系我： liyulai@qq.com
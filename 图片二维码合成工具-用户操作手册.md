# 用户操作手册 · 图片二维码合成工具

> 适用对象：普通使用者
> 版本：2.0（2026-08）

---

## 1. 工具用途

把「一张底图」和「一批二维码图片」批量合成：每张二维码单独合成到底图上，生成一批最终图片，最后打包成一个 ZIP 压缩包供你下载。

- 一个文件、双击就能用，无需安装任何软件
- 全程在你自己的电脑里处理，图片不会上传到任何服务器
- 底图 + 二维码合成后，可对二维码进行移动、缩放、旋转、四边裁剪

**典型场景**：门店宣传海报需要「同一张底图 + 不同门店的二维码」，一次批量生成全部门店的海报。

---

## 2. 使用前准备

1. 用 Chrome 或 Edge 浏览器打开文件夹里的 `index.html`（双击即可）。
2. 准备好一张**底图**（海报/背景图）。
3. 准备好一批**二维码图片**（每个二维码一张图）。

---

## 3. 操作步骤

### 第 1 步：上传底图
- 点击「上传底图」选择文件，或直接把图片**拖拽到右侧画布**。
- 底图仅支持 1 张，格式支持 PNG / JPG / WebP 等常见格式。
- 上传成功后画布会显示底图，并自动缩放到合适大小。

### 第 2 步：批量添加二维码
- 点击「批量添加二维码」，一次可多选。
- 导入成功后，左侧会自动出现「位置调整」和「二维码剪裁」两个面板。

### 第 3 步：调整二维码（三种方式任选）
- **画布上直接操作**：
  - 拖动二维码 = 移动位置
  - 拖动右下角橙色方块 = 等比缩放
  - 拖动四边绿色圆点 = 从该边向内裁剪
- **输入框精确输入**：横向 / 纵向 / 缩放 / 旋转，以及四边裁剪像素值。
- **方向键微调**：选中二维码后按键盘 ↑↓←→，每次移动 1 像素。

### 第 4 步：（可选）添加固定二维码
- 展开「固定二维码（可选）」，添加在**每张**导出结果中固定显示的二维码（如品牌公众号码、固定客服码）。
- 最多 2 个，不配置不影响导出。

### 第 5 步：预览并导出
- 点「缩略图总览」可一次预览全部合成效果。
- 点「一键批量导出（ZIP）」，浏览器自动下载压缩包。

---

## 4. 导出规则

| 项目 | 规则 |
|---|---|
| 图片格式 | 与底图保持一致（底图是 PNG 导出 PNG，是 JPG 导出 JPG） |
| 图片分辨率 | 与底图原始分辨率完全一致 |
| 文件命名 | 取对应二维码的原始文件名（后缀会自动对齐底图格式） |
| 重名处理 | 同名文件自动追加序号（如 `码 (2).jpg`），不会互相覆盖 |
| 压缩包名称 | `二维码合成结果-年月日-时分秒.zip` |
| 画质策略 | PNG 无损；若底图为 JPG/WebP，自动用最高质量（0.99）导出 |

> 提示：如需完全无损，请用 PNG 格式的底图。

---

## 5. 常见问题

**Q：需要联网吗？**
不需要。所有处理都在浏览器本地完成，断网也能用。

**Q：图片会泄露吗？**
不会。全程本地处理，无任何上传。

**Q：支持多少张二维码？**
动态二维码最多 200 张，固定二维码最多 2 个。

**Q：图片大小有限制吗？**
底图不超过 50MB、二维码不超过 30MB，且像素不超过 3000 万。

**Q：二维码拖到画布外面找不到了？**
工具已限制边界，二维码始终会保留至少一部分在画布内，不会完全拖丢。点「重置位置」可一键回到居中。

**Q：导出文件名的后缀和图片实际格式会不一致吗？**
不会（2.0 已修复）。导出图片格式跟随底图，文件名后缀会自动对齐，例如底图是 PNG 时，即使二维码文件名是 `.jpg`，导出后也会自动改成 `.png`。

**Q：怎么让二维码始终出现在每张图里？**
用「固定二维码（可选）」功能，而不是「批量添加二维码」。

---

## 6. 快捷键速查

| 操作 | 方式 |
|---|---|
| 移动二维码 | 画布拖动 / 键盘 ↑↓←→（1px） |
| 缩放二维码 | 拖动右下角橙色方块 / 输入缩放值 |
| 裁剪二维码 | 拖动四边绿色圆点 / 输入裁剪值 / ＋－按钮 |
| 放大预览 | 滚轮向上 / 点「＋」 |
| 缩小预览 | 滚轮向下 / 点「−」 |
| 适应屏幕 | 点「适应屏幕」按钮 |

## 如有侵权或意见，可通过邮件联系我： liyulai@qq.com